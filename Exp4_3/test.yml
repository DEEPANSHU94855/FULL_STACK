config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 20

scenarios:
  - flow:
      - post:
          url: "/api/seats/lock"
          json:
            seatId: "C1"
            userId: "{{ $randomString() }}"